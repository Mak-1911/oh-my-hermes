name: Pages

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/pages.yml"
      - "site/**"
      - "README.md"
      - "docs/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install package
        run: python -m pip install -e .
      - name: Check generated docs
        run: python -m omh.cli docs workflows --check
      - name: Validate harness catalog
        run: python -m omh.cli harness validate
      - name: Configure Pages
        uses: actions/configure-pages@v6
        with:
          enablement: true
      - name: Prepare static site
        run: |
          rm -rf _site
          mkdir -p _site/assets
          # site/assets is self-contained: demo videos (webm/mp4), the
          # character mask, and the boot banner are tracked under site/.
          cp -R site/. _site/
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: "_site"

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url || steps.deployment_retry.outputs.page_url || steps.deployment_final_retry.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
        continue-on-error: true
      - name: Wait before Pages deploy retry
        if: steps.deployment.outcome == 'failure'
        run: sleep 60
      - name: Retry deploy after transient Pages failure
        if: steps.deployment.outcome == 'failure'
        id: deployment_retry
        uses: actions/deploy-pages@v5
        continue-on-error: true
      - name: Wait before final Pages deploy retry
        if: steps.deployment.outcome == 'failure' && steps.deployment_retry.outcome == 'failure'
        run: sleep 120
      - name: Final retry deploy after transient Pages failure
        if: steps.deployment.outcome == 'failure' && steps.deployment_retry.outcome == 'failure'
        id: deployment_final_retry
        uses: actions/deploy-pages@v5
